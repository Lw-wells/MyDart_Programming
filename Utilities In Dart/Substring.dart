// The substring() method extracts a portion of a string by specifying start and end indices.

void main() {
  String text = "Hello, World! this is a substring example.";

  String submessage =
      text.substring(3, 5); // Extracts characters from index 3 to 7

  print(submessage);
}
